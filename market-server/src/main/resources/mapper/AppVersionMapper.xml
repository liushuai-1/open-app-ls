<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.approval.mapper.AppVersionMapper">

    <resultMap id="BaseResultMap" type="com.xxx.it.works.wecode.v2.modules.approval.entity.AppVersionEntity">
        <id column="id" property="id"/>
        <result column="app_id" property="appId"/>
        <result column="version_desc_cn" property="versionDescCn"/>
        <result column="version_desc_en" property="versionDescEn"/>
        <result column="version_code" property="versionCode"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_by" property="lastUpdateBy"/>
        <result column="last_update_time" property="lastUpdateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, app_id, version_desc_cn, version_desc_en, version_code, tenant_id,
        status, create_by, create_time, last_update_by, last_update_time
    </sql>

    <!-- ==================== selectById ==================== -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM openplatform_app_version_t
        WHERE id = #{id}
    </select>

    <!-- ==================== selectByIds ==================== -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM openplatform_app_version_t
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- ==================== updateStatus ==================== -->
    <update id="updateStatus">
        UPDATE openplatform_app_version_t
        SET status = #{status},
            last_update_time = NOW(3)
        WHERE id = #{id}
    </update>

</mapper>
