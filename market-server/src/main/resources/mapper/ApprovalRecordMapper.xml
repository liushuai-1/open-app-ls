<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.approval.mapper.ApprovalRecordMapper">

    <resultMap id="BaseResultMap" type="com.xxx.it.works.wecode.v2.modules.approval.entity.ApprovalRecord">
        <id column="id" property="id"/>
        <result column="business_type" property="businessType"/>
        <result column="business_id" property="businessId"/>
        <result column="applicant_id" property="applicantId"/>
        <result column="applicant_name" property="applicantName"/>
        <result column="status" property="status"/>
        <result column="current_node" property="currentNode"/>
        <result column="combined_nodes" property="combinedNodes"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="last_update_by" property="lastUpdateBy"/>
        <result column="completed_at" property="completedAt"/>
    </resultMap>

    <resultMap id="PublishedAppResultMap" type="com.xxx.it.works.wecode.v2.modules.approval.entity.PublishedAppDto">
        <id column="app_pk_id" property="appPkId"/>
        <result column="app_id" property="appId"/>
        <result column="app_name_cn" property="appNameCn"/>
        <result column="app_name_en" property="appNameEn"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="version_id" property="versionId"/>
        <result column="version_code" property="versionCode"/>
    </resultMap>

    <resultMap id="PropertyResultMap" type="com.xxx.it.works.wecode.v2.modules.approval.entity.PropertyEntity">
        <id column="parent_id" property="parentId"/>
        <result column="property_value" property="propertyValue"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, business_type, business_id, applicant_id, applicant_name,
        status, current_node, combined_nodes,
        create_time, last_update_time, create_by, last_update_by, completed_at
    </sql>

    <!-- ==================== selectById ==================== -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM openplatform_v2_approval_record_t
        WHERE id = #{id}
    </select>

    <!-- ==================== selectPendingList (单表) ==================== -->
    <select id="selectPendingList" resultMap="BaseResultMap">
        SELECT r.id, r.business_type, r.business_id, r.applicant_id,
               r.status, r.current_node, r.create_time, r.last_update_time
        FROM openplatform_v2_approval_record_t r
        WHERE r.status = 0
          AND r.business_type = 'app_version_publish'
        ORDER BY r.last_update_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- ==================== countPendingList ==================== -->
    <select id="countPendingList" resultType="long">
        SELECT COUNT(*)
        FROM openplatform_v2_approval_record_t
        WHERE status = 0
          AND business_type = 'app_version_publish'
    </select>

    <!-- ==================== selectPublishedList (app_t main + subquery) ==================== -->
    <select id="selectPublishedList" resultMap="PublishedAppResultMap">
        SELECT a.id AS app_pk_id, a.app_id, a.app_name_cn, a.app_name_en,
               a.create_time, a.last_update_time,
               v.id AS version_id, v.version_code
        FROM openplatform_app_t a
        INNER JOIN (
            SELECT v2.app_id, MAX(v2.version_code) AS max_version_code
            FROM openplatform_app_version_t v2
            WHERE v2.status = 4
            GROUP BY v2.app_id
        ) latest ON a.id = latest.app_id
        INNER JOIN openplatform_app_version_t v
          ON a.id = v.app_id AND v.version_code = latest.max_version_code
        WHERE a.status = 1 AND a.app_type = 1
        ORDER BY a.last_update_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- ==================== countPublishedList ==================== -->
    <select id="countPublishedList" resultType="long">
        SELECT COUNT(DISTINCT v.app_id)
        FROM openplatform_app_version_t v
        INNER JOIN openplatform_app_t a ON v.app_id = a.id
        WHERE v.status = 4
          AND a.status = 1
          AND a.app_type = 1
    </select>

    <!-- ==================== update ==================== -->
    <update id="update">
        UPDATE openplatform_v2_approval_record_t
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="currentNode != null">current_node = #{currentNode},</if>
            <if test="completedAt != null">completed_at = #{completedAt},</if>
            <if test="lastUpdateTime != null">last_update_time = #{lastUpdateTime},</if>
            <if test="lastUpdateBy != null">last_update_by = #{lastUpdateBy},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- ==================== selectApplicantByVersionId ==================== -->
    <select id="selectApplicantByVersionId" resultType="java.lang.String">
        SELECT applicant_id
        FROM openplatform_v2_approval_record_t
        WHERE business_id = #{versionId}
          AND status = 1
          AND business_type = 'app_version_publish'
        ORDER BY id DESC
        LIMIT 1
    </select>

    <!-- ==================== selectVersionAbilityIds ==================== -->
    <select id="selectVersionAbilityIds" resultType="string">
        SELECT property_value
        FROM openplatform_app_version_p_t
        WHERE parent_id = #{versionId}
          AND property_name = 'abilityIds'
    </select>

    <!-- ==================== selectThirdPartyAppId（查询应用编码 eamap_app_code） ==================== -->
    <select id="selectThirdPartyAppId" resultType="string">
        SELECT property_value
        FROM openplatform_app_p_t
        WHERE parent_id = #{appPkId} AND property_name = 'eamap_app_code'
    </select>

    <!-- ==================== selectVersionAbilityIdsBatch ==================== -->
    <select id="selectVersionAbilityIdsBatch" resultMap="PropertyResultMap">
        SELECT parent_id, property_value
        FROM openplatform_app_version_p_t
        WHERE parent_id IN
        <foreach collection="versionIds" item="vid" open="(" separator="," close=")">
            #{vid}
        </foreach>
        AND property_name = 'abilityIds'
    </select>

    <!-- ==================== selectApplicantsByVersionIds ==================== -->
    <select id="selectApplicantsByVersionIds" resultMap="PropertyResultMap">
        SELECT t.business_id AS parent_id, t.applicant_id AS property_value
        FROM openplatform_v2_approval_record_t t
        INNER JOIN (
            SELECT business_id, MAX(id) AS max_id
            FROM openplatform_v2_approval_record_t
            WHERE business_id IN
            <foreach collection="versionIds" item="vid" open="(" separator="," close=")">
                #{vid}
            </foreach>
            AND status = 1
            AND business_type = 'app_version_publish'
            GROUP BY business_id
        ) latest ON t.id = latest.max_id
    </select>

    <!-- ==================== selectEamapAppCodesByAppIds ==================== -->
    <select id="selectEamapAppCodesByAppIds" resultMap="PropertyResultMap">
        SELECT parent_id, property_value
        FROM openplatform_app_p_t
        WHERE parent_id IN
        <foreach collection="appIds" item="appId" open="(" separator="," close=")">
            #{appId}
        </foreach>
        AND property_name = 'eamap_app_code'
    </select>

</mapper>
